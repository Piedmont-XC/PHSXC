# PHSXC Logo Update

Upload these files to the root of the `Piedmont-XC/PHSXC` repository.

Files included:

- `index.html` — updated main page header with the PHS logo
- `logo.css` — new CSS file that controls the logo size and layout
- `assets/PHS-logo.png` — the logo image

Expected repo structure after upload:

```text
PHSXC/
  index.html
  logo.css
  assets/
    PHS-logo.png
```

The main site should publish at:

```text
https://piedmont-xc.github.io/PHSXC/
```
