// PHSXC Coach Runner Log v30
const GOOGLE_APPS_SCRIPT_URL="https://script.google.com/macros/s/AKfycbxrZU9YRCoi1giUkmyski0VrBzKpI1Tfrk--TYInwjK48yo7SCaT0I66mHbuW1Tc0Fp/exec";const selectedDateEl=document.getElementById("selectedDate"),statusEl=document.getElementById("coachLogStatus"),resultsEl=document.getElementById("coachLogResults");const athleteId=sessionStorage.getItem("phsxcCoachAthleteId")||"",storedDisplayName=sessionStorage.getItem("phsxcCoachDisplayName")||"",coachCode=sessionStorage.getItem("phsxcCoachCode")||"";function localISODate(date=new Date()){const tzOffset=date.getTimezoneOffset()*60000;return new Date(date.getTime()-tzOffset).toISOString().slice(0,10)}function parseISODateAsLocal(iso){const[y,m,d]=iso.split("-").map(Number);return new Date(y,m-1,d)}function shiftSelectedDate(days){const base=selectedDateEl.value?parseISODateAsLocal(selectedDateEl.value):new Date();base.setDate(base.getDate()+days);selectedDateEl.value=localISODate(base)}function formatNumber(value,digits=1){const num=Number(value||0);if(!Number.isFinite(num))return"0";if(Math.abs(num-Math.round(num))<.001)return String(Math.round(num));return num.toFixed(digits)}function escapeHTML(str){return String(str||"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[char]))}function formatDisplayDate(isoOrText){if(!isoOrText)return"";const raw=String(isoOrText);const m=raw.match(/^(\d{4})-(\d{2})-(\d{2})$/);if(!m)return raw;const d=new Date(Number(m[1]),Number(m[2])-1,Number(m[3]));return d.toLocaleDateString(undefined,{weekday:"short",month:"short",day:"numeric"})}function requestViaJSONP(paramsObj){return new Promise((resolve,reject)=>{const callback=`phsxcCoachLogCallback_${Date.now()}`;const params=new URLSearchParams({...paramsObj,callback,v:Date.now().toString()});const scriptUrl=`${GOOGLE_APPS_SCRIPT_URL}${GOOGLE_APPS_SCRIPT_URL.includes("?")?"&":"?"}${params.toString()}`;const script=document.createElement("script");const timeout=setTimeout(()=>{cleanup();reject(new Error("Request timed out. Please try again."))},15000);function cleanup(){clearTimeout(timeout);delete window[callback];if(script.parentNode)script.parentNode.removeChild(script)}window[callback]=function(response){cleanup();if(response&&response.ok)resolve(response);else reject(new Error(response?.error||"Could not load runner log."))};script.onerror=function(){cleanup();reject(new Error("Could not connect. Check connection and try again."))};script.src=scriptUrl;document.body.appendChild(script)})}function init(){selectedDateEl.value=localISODate();if(storedDisplayName)document.getElementById("coachLogTitle").textContent=`${storedDisplayName}’s Workout Log`;if(!athleteId||!coachCode){statusEl.textContent="Coach View is not unlocked. Return to the Summer Team Board and unlock Coach View.";statusEl.className="form-status error";return}document.getElementById("prevWeekBtn").addEventListener("click",()=>{shiftSelectedDate(-7);loadCoachLog()});document.getElementById("thisWeekBtn").addEventListener("click",()=>{selectedDateEl.value=localISODate();loadCoachLog()});document.getElementById("nextWeekBtn").addEventListener("click",()=>{shiftSelectedDate(7);loadCoachLog()});selectedDateEl.addEventListener("change",loadCoachLog);document.getElementById("refreshCoachLogBtn").addEventListener("click",loadCoachLog);loadCoachLog()}async function loadCoachLog(){statusEl.textContent="Loading runner log…";statusEl.className="form-status";resultsEl.hidden=true;try{const response=await requestViaJSONP({action:"getCoachRunnerLog",coachCode,athleteId,selectedDate:selectedDateEl.value||localISODate()});renderResults(response);statusEl.textContent=""}catch(err){statusEl.textContent=err.message||"Could not load runner log.";statusEl.className="form-status error"}}function renderResults(response){resultsEl.hidden=false;const displayName=response.displayName||storedDisplayName||"Runner";document.getElementById("coachLogTitle").textContent=`${displayName}’s Workout Log`;document.getElementById("resultsTitle").textContent=`${displayName}’s Workout Log`;document.getElementById("resultsSubtitle").textContent=response.periodLabel||"";const day=response.dayTotals||{},week=response.weekTotals||{},summer=response.summerTotals||{};document.getElementById("daySummary").innerHTML=`${formatNumber(day.minutes,0)} min<br>${formatNumber(day.miles,2)} mi<br>${day.entries||0} entries`;document.getElementById("weekSummary").innerHTML=`${formatNumber(week.minutes,0)} min<br>${formatNumber(week.miles,2)} mi<br>${week.entries||0} entries`;document.getElementById("summerSummary").innerHTML=`${formatNumber(summer.minutes,0)} min<br>${formatNumber(summer.miles,2)} mi<br>${summer.entries||0} entries`;document.getElementById("strengthSummary").innerHTML=`${day.strengthSessions||0} day · ${week.strengthSessions||0} week · ${summer.strengthSessions||0} summer`;const entries=response.weekEntries||[];document.getElementById("entriesHeading").textContent=entries.length?`Entries This Week (${entries.length})`:"Entries This Week";const list=document.getElementById("entriesList");if(!entries.length){list.innerHTML=`<p class="details">No entries found for this week.</p>`;return}list.innerHTML=entries.map(entry=>`<article class="entry-card ${entry.normalizedDate===response.selectedDate?"selected-day-entry":""}"><h3>${escapeHTML(formatDisplayDate(entry.normalizedDate||entry.date))}</h3><p class="entry-main">${formatNumber(entry.timeRun,0)} min · ${formatNumber(entry.distanceRun,2)} mi</p><p class="entry-detail">Effort: ${escapeHTML(entry.effort||"")}/10 · Felt: ${escapeHTML(entry.feel||"")}</p><p class="entry-detail">Strength: ${escapeHTML(entry.strength||"No")}${entry.entryType==="Additional"?" · Additional Entry":""}</p>${entry.exercises?`<p class="entry-detail">Exercises: ${escapeHTML(entry.exercises)}</p>`:""}${entry.notes?`<p class="entry-notes">${escapeHTML(entry.notes)}</p>`:""}</article>`).join("")}init();
